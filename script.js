// Smooth Scrolling Navigation\ndocument.querySelectorAll('a[href^="#"]').forEach(anchor => {\n    anchor.addEventListener('click', function (e) {\n        e.preventDefault();\n        document.querySelector(this.getAttribute('href')).scrollIntoView({\n            behavior: 'smooth'\n        });\n    });\n});\n\n// Form Handling\ndocument.querySelector('form').addEventListener('submit', function (e) {\n    e.preventDefault();\n    const formData = new FormData(this);\n    const name = formData.get('name'); // assuming there's an input named 'name'\n    console.log(`Form submitted. Name: ${name}`);\n    // You can add further handling (e.g., AJAX call here)\n});\n\n// Dynamic Greeting Functionality\nconst greetingElement = document.getElementById('greeting'); // assuming there's an element with id 'greeting'\nconst currentHour = new Date().getUTCHours();\nlet greeting;\n\nif (currentHour < 12) {\n    greeting = "Good morning!";\n} else if (currentHour < 18) {\n    greeting = "Good afternoon!";\n} else {\n    greeting = "Good evening!";\n}\n\ngreetingElement.textContent = greeting;